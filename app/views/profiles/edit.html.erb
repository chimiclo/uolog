<h1>プロフィール編集</h1>

<%= form_with model: @profile, url: profile_path, method: :patch do |f| %>
  <div>
    <%= f.label :name, "名前" %><br>
    <%= f.text_field :name %>
  </div>

  <div>
    <%= f.label :bio, "自己紹介" %><br>
    <%= f.text_area :bio %>
  </div>

  <%= f.submit "更新する" %>
<% end %>
