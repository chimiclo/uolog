<div class="catch-form-container">
  <h1 class="form-title">釣果を編集</h1>

  <%= form_with(model: @catch, local: true) do |form| %>
    <%= render "form", form: form %>
  <% end %>

  <div class="form-back">
    <%= link_to "戻る", catch_path(@catch), class: "btn-back" %>
  </div>
</div>
