<div class="catch-form-container catches-form-page">
  <h1 class="form-title">釣果を編集</h1>

  <%= render "form", catch: @catch %>

  <div class="form-back">
    <%= link_to "詳細へ戻る", catch_path(@catch), class: "btn-back" %>
  </div>
</div>

<%= javascript_include_tag "map", defer: true %>
